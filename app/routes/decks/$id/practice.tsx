import * as React from "react";

// TODO: practice loop for given deck
// - getNextPracticeEntry (loader)
// - if nothing, some CTA (practice finished)
// - otherwise, ...

export default function DefaultComponent() {
  return <div>/decks/$id/practice</div>;
}
